export const slapshotAiConfig = {
  dailyReportTime: '09:20',
  weeklyReportTime: '09:30',
  weeklyReportDay: 1, // 1 = Monday (in standard JS getDay(), 1 is Monday, 0 is Sunday, but we will use date-fns to be safe)
};

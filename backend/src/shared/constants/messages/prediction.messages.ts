export const PredictionMessages = {
  PREDICTION_CREATED_SUCCESS: 'Prediction created successfully',
} as const;

export const PredictionErrors = {
  MATCH_NOT_FOUND: 'Match not found',
  MATCH_ALREADY_STARTED: 'Match already started',
} as const;
